name: smart_parking_pos
description: Smart car parking management POS and admin dashboard.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  go_router: ^14.2.7
  dio: ^5.7.0
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.0.5
  image_picker: ^1.1.2
  google_fonts: ^6.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
